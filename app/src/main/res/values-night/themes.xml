<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CurrencyTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/cardBackground</item>
        <item name="android:navigationBarColor">@color/cardBackground</item>
        <item name="android:windowBackground">@color/backgroundGray</item>
    </style>
</resources>
